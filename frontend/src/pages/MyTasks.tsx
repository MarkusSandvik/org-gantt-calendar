import { PagePlaceholder } from "../components/layout/PagePlaceholder";

export function MyTasks() {
  return (
    <PagePlaceholder
      title="My Tasks"
      phaseNote="Owner/contributor task views and filters arrive in Phase 14."
    />
  );
}
